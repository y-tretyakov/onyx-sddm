# Stage 1.1 Review Follow-ups (CR-1…CR-5) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Применить замечания и рекомендации code review Stage 1.1 (`docs/code-reviews/Stage 1.1 Code Review.md`, veredict APPROVED WITH MINOR FINDINGS), переименовать ревью-файл по конвенции, синхронизировать документацию и tooling, зафиксировать процессные правила.

**Architecture:** Изменения касаются документации (scale ownership, CI evidence), валидатора (`validate.sh` — слоёная проверка), preview-инфраструктуры (`scripts/preview.sh` + моки `sddm`/`config`) и статусных поверхностей (CHANGELOG). Код темы (`theme/onyx/*.qml`) НЕ меняется — ревью не выявило дефектов реализации.

**Tech Stack:** bash (`validate.sh`, `scripts/preview.sh`), Qt6/QML preview (`qml6`/`qml`), Markdown (docs).

**Spec:** `docs/code-reviews/Stage 1.1 Code Review.md` (CR-1…CR-5, R1…R4, §7, §18) + `AGENTS.md` §3.1–3.3, §8 + `docs/ARCHITECTURE.md` §5.1.

## Global Constraints

- Трогаем: `docs/code-reviews/…` (переименование — только git mv, по явному указанию пользователя), `docs/ARCHITECTURE.md`, `docs/AGENTS.md`, `validate.sh`, новый `scripts/preview.sh`, новое `preview/` (Моск-файлы), `CHANGELOG.md`, этот план. НЕ трогаем: `theme/onyx/**` (код темы), `metadata.desktop`, `theme.conf`, `README.md`, `docs/SPECS/**`, CI workflow.
- Строго: `docs/code-reviews/` — контент ревью не редактируем; только rename файла `Stage 1.1 Code Review.md` → `2026-09-08-stage-1.1.md`.
- Все изменения — в ветке `stage/1.1-review-followups`, ответвлённой от `dev`.
- Commit messages по AGENTS.md §3: для изменений вне ROADMAP-stages используем префикс `docs(...)`/`chore(...)`/`feat(...)`, не `stage(X.Y)`.
- Версия НЕ бумпится (это не этап ROADMAP; запись в CHANGELOG — реестр follow-up, не новый stage).
- Без debug-мусора, без лишних файлов.

---

### Task 1: Rename review file to convention

**Files:**
- Rename: `docs/code-reviews/Stage 1.1 Code Review.md` → `docs/code-reviews/2026-09-08-stage-1.1.md`

- [ ] **Step 1: git mv**

```bash
git mv "docs/code-reviews/Stage 1.1 Code Review.md" "docs/code-reviews/2026-09-08-stage-1.1.md"
```

- [ ] **Step 2: Verify**

```bash
git status --short | grep "code-reviews"
```
Expected: rename staged (old file deleted, new file added), специальный символ пробела обработан корректно git mv (нет копии `Stage 1.1 Code Review.md`).

- [ ] **Step 3: Commit**

```bash
git add -A docs/code-reviews/
git commit -m "docs: rename stage 1.1 review to date-based convention

- Stage 1.1 Code Review.md -> 2026-09-08-stage-1.1.md (AGENTS.md §3.3)"
```

---

### Task 2: Sync ARCHITECTURE.md scale ownership (CR-1/R1, HIGH)

**Files:**
- Modify: `docs/ARCHITECTURE.md` §5.1 (строки ~164–173), строку версии документа (строка 7)

- [ ] **Step 1: Rewrite §5.1**

Заменить код в `docs/ARCHITECTURE.md` §5.1 (сейчас там `// В ThemeState.qml readonly property real s: Screen.height / 768`) на утверждённый ревью текст (рекомендация §7 ревью):

```markdown
### 5.1 Scaling

The scale factor is calculated only by the visual root:

```qml
readonly property real s: Screen.height / 768
```

`Main.qml` owns the screen-derived scale.

`ThemeState` receives `s` from the root and exposes it as global theme state.

Visual components receive the scale through composition/state and must not independently calculate screen scale.
```

- [ ] **Step 2: Bump document version**

Строка 7: `Version of this document: 0.2.0 · 2026-09-08` → `0.2.1 · 2026-09-08`.

- [ ] **Step 3: Verify**

```bash
grep -n "ThemeState receives" docs/ARCHITECTURE.md && grep -n "0.2.1" docs/ARCHITECTURE.md | head -1
```
Expected: оба совпадения присутствуют, и НЕ осталось упоминаний `readonly property real s` внутри § ThemeState.qml (кроме контекста «receives»).

- [ ] **Step 4: Commit**

```bash
git add docs/ARCHITECTURE.md
git commit -m "docs: sync scale ownership model in ARCHITECTURE (CR-1/R1)

- Main.qml owns screen-derived scale; ThemeState receives s
- components consume s via composition/state, never compute it"
```

---

### Task 3: Layered validation in validate.sh (CR-2/R3, MEDIUM)

**Files:**
- Modify: `validate.sh` (полная замена)

- [ ] **Step 1: Rewrite validate.sh**

Переписать `validate.sh` на слоёную проверку (4 слоя). Точный целевой код:

```bash
#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
THEME_DIR="${1:-${ROOT_DIR}/theme/onyx}"

usage() {
    cat <<EOF
Usage: $0 [THEME_DIR] [--help]

Validates the Onyx theme file set (layered):
  1. required files present
  2. metadata.desktop fields valid
  3. theme.conf keys present
  4. QML syntax (qmllint, if available)

  THEME_DIR  theme directory to check (default: theme/onyx)
  -h, --help  show this help and exit
EOF
}

case "${1:-}" in
    -h|--help) usage; exit 0 ;;
esac

status=0

fail() { echo "  - $1" >&2; status=1; }

# --- Layer 1: required files ---
REQUIRED_FILES=(
    "Main.qml"
    "ThemeState.qml"
    "translations.js"
    "PROVENANCE.txt"
    "metadata.desktop"
    "theme.conf"
)

missing=()
for f in "${REQUIRED_FILES[@]}"; do
    if [[ ! -e "${THEME_DIR}/${f}" ]]; then
        missing+=("${f}")
    fi
done
if [[ ${#missing[@]} -gt 0 ]]; then
    echo "FAIL: missing required files:" >&2
    printf '  - %s\n' "${missing[@]}" >&2
    exit 1
fi

# --- Layer 2: metadata.desktop ---
MD="${THEME_DIR}/metadata.desktop"
if ! grep -q '^\[SddmGreeterTheme\]' "${MD}"; then
    fail "metadata.desktop: missing [SddmGreeterTheme] section"
fi
if ! grep -q '^QtVersion=6$' "${MD}"; then
    fail "metadata.desktop: QtVersion must be exactly 6"
fi
main_script="$(grep '^MainScript=' "${MD}" | head -n1 | cut -d= -f2-)"
if [[ -z "${main_script}" ]] || [[ ! -e "${THEME_DIR}/${main_script}" ]]; then
    fail "metadata.desktop: MainScript '${main_script}' not found in theme dir"
fi
config_file="$(grep '^ConfigFile=' "${MD}" | head -n1 | cut -d= -f2-)"
if [[ -z "${config_file}" ]] || [[ ! -e "${THEME_DIR}/${config_file}" ]]; then
    fail "metadata.desktop: ConfigFile '${config_file}' not found in theme dir"
fi

# --- Layer 3: theme.conf ---
TC="${THEME_DIR}/theme.conf"
if ! grep -q '^\[General\]' "${TC}"; then
    fail "theme.conf: missing [General] section"
fi
for key in type color bgColor; do
    if ! grep -Eq "^${key}=" "${TC}"; then
        fail "theme.conf: missing key '${key}'"
    fi
done

# --- Layer 4: QML syntax ---
QML_FILES=()
while IFS= read -r f; do QML_FILES+=("$f"); done < <(find "${THEME_DIR}" -name '*.qml' -type f)
if [[ ${#QML_FILES[@]} -eq 0 ]]; then
    fail "no *.qml files found in theme dir"
fi

QMLINT=""
for cand in qmllint6 qmllint /usr/lib/qt6/bin/qmllint; do
    if command -v "${cand}" >/dev/null 2>&1 || [[ -x "${cand}" ]]; then
        QMLINT="${cand}"
        break
    fi
done

if [[ -z "${QMLINT}" ]]; then
    echo "WARNING: qmllint not found; skipping QML syntax check" >&2
else
    for f in "${QML_FILES[@]}"; do
        if ! "${QMLINT}" "${f}" >/dev/null 2>&1; then
            if ! "${QMLINT}" --unqualified=info --import=info "${f}" >/dev/null 2>&1; then
                fail "QML syntax/type error in ${f}"
            fi
        fi
    done
fi

if [[ ${status} -eq 0 ]]; then
    echo "OK: ${THEME_DIR}"
    exit 0
fi

echo "FAIL: ${THEME_DIR}" >&2
exit 1
```

- [ ] **Step 2: Verify pass on valid theme**

Run:
```bash
./validate.sh
```
Expected: `OK: .../theme/onyx`, exit 0.

- [ ] **Step 3: Verify failure on broken metadata**

Run:
```bash
mkdir -p /tmp/onyx-broken
cp theme/onyx/Main.qml theme/onyx/ThemeState.qml theme/onyx/translations.js theme/onyx/PROVENANCE.txt theme/onyx/theme.conf /tmp/onyx-broken/
printf '[SddmGreeterTheme]\nQtVersion=5\nMainScript=Missing.qml\nConfigFile=theme.conf\n' > /tmp/onyx-broken/metadata.desktop
./validate.sh /tmp/onyx-broken; echo "exit=$?"
```
Expected: FAIL + минимум 3 строки (QtVersion≠6, MainScript not found, QtVersion... ), reset; `exit=1`. (Точный состав: QtVersion=5 → fail; Missing.qml → fail.)

- [ ] **Step 4: Verify failure on broken QML**

Run:
```bash
mkdir -p /tmp/onyx-badqml
cp theme/onyx/*.js theme/onyx/*.desktop theme/onyx/PROVENANCE.txt theme/onyx/*.conf /tmp/onyx-badqml/
printf 'Rectangle {\n    completely invalid\n}\n' > /tmp/onyx-badqml/Main.qml
cp theme/onyx/ThemeState.qml /tmp/onyx-badqml/
./validate.sh /tmp/onyx-badqml; echo "exit=$?"
```
Expected: FAIL + «QML syntax/type error in Main.qml»; `exit=1`. (Если qmllint не найден — WARNING и exit 0; в этом окружении qmllint присутствует.)

- [ ] **Step 5: Commit**

```bash
git add validate.sh
git commit -m "chore(validate): layered validation (CR-2/R3)

- Layer 1: required files
- Layer 2: metadata.desktop fields (QtVersion=6, MainScript/ConfigFile exist)
- Layer 3: theme.conf keys
- Layer 4: qmllint syntax check when available (flat fallback to --unqualified=info --import=info)"
```

---

### Task 4: Preview runner + mocks (CR-4/R4, MEDIUM)

**Files:**
- Create: `scripts/preview.sh`
- Create: `preview/MockSddm.qml`
- Create: `preview/MockConfig.qml`

**Skills for executor:** `sddm-theme` + `qt-qml` (обязательно). Изучить, как правильно превьюрить SDDM-тему Qt6 без живого greeter.

- [ ] **Step 1: Create scripts/preview.sh**

Bash-скрипт с `set -euo pipefail`. Ключевые функции:
- auto-detect QML runner: `qml6` → `qml` (НЕ `qmlscene` — Qt5.15 на машине, несовместим с versionless `import QtQuick`).
- опции: `--mock` (инжектить моки), `-W/--width`, `-H/--height` (детерминированный размер окна по возможности: проверить `qml6 --help` на предмет флагов размера; если нет — нотировать, что размер берётся из текущего экрана), `-o/--output FILE.png` (скриншот ЧЕРЕЗ grabToImage-wrapper если `qml6 --help` не даёт нативного `--output`), `-h/--help`.
- preview-safe: без живого `sddm` тема не падает (graceful degradation — уже в ThemeState.isPreview).
- `-o` реализуется через временный wrapper `.qml` в `/tmp` (Loader на `theme/onyx/Main.qml` + `grabToImage` по таймеру), генерируемый скриптом при создании; временный файл удаляется после. НЕ создаём постоянных файлов в `theme/**`.

Вернуть в отчёте: каким механизмом реализован размер окна и скриншот (флаги qml6 / wrapper), что выбрано и почему.

- [ ] **Step 2: Create preview/MockSddm.qml**

QtObject-заглушка SDDM-объекта для превью (по sddm-theme: `hostName`, при необходимости `login()`, `powerOff()`, `reboot()` — как стабы, без побочных эффектов). Комментарии-pragmatic минимальные либо отсутствуют.

```qml
import QtQuick

QtObject {
    property string hostName: "onyx-preview"
}
```

- [ ] **Step 3: Create preview/MockConfig.qml**

QtObject-заглушка `config` (theme.conf) для превью:

```qml
import QtQuick

QtObject {
    property string bgColor: "#202020"
}
```

- [ ] **Step 4: Wire --mock (best-effort)**

Если выбранный qml runner позволяет инжектить context-properties (`sddm`, `config`) извне — использовать. Если чистыми средствами `qml6`/`qml` это невозможно (ожидаемо — эти объекты делает SDDM как context-properties) — `--mock` выдаёт понятный warning: «full sddm injection requires the greeter context; mocks provided at preview/MockSddm.qml + preview/MockConfig.qml for future harness», после чего запускает превью (graceful path). Это допустимо и фиксируется в отчёте.

- [ ] **Step 5: Sanity — qml6 preview без падений**

Run:
```bash
./scripts/preview.sh --mock ; echo "exit=$?"
```
Expected: превью-окно открылось; после закрытия exit 0. При невозможности показывать окно в этой сессии (нет дисплея) — использовать timeout и зафиксировать, что процесс стартовал без ошибок (повторить методику проверки из stage 1.1 Task 2).

- [ ] **Step 6: Sanity — validate.sh не сломан**

Run:
```bash
./validate.sh
```
Expected: OK, exit 0.

- [ ] **Step 7: Commit**

```bash
git add scripts/preview.sh preview/MockSddm.qml preview/MockConfig.qml
git commit -m "feat(preview): preview runner + sddm/config mocks (CR-4/R4)

- scripts/preview.sh: qml6 auto-detect, --mock/-W/-H/-o options
- preview/MockSddm.qml + preview/MockConfig.qml fixtures
- graceful preview path via ThemeState.isPreview"
```

---

### Task 5: CI evidence standard (CR-3/R2, HIGH)

**Files:**
- Modify: `AGENTS.md` §8 (Definition of Done)

- [ ] **Step 1: Add CI evidence requirement to §8**

В §8 (Definition of Done) добавить пункт (после существующего пункта 7):

```markdown
8. Отчёт о закрытии содержит явные CI-доказательства:
   - имя workflow,
   - commit SHA,
   - ссылку/ID run,
   - результат по каждому job (PASS/FAIL).

   Голое заявление «CI passed» без ссылок — не считается доказательством (follow-up CR-3).
```

- [ ] **Step 2: Verify**

```bash
grep -n "CR-3" AGENTS.md
```
Expected: строка присутствует.

- [ ] **Step 3: Commit**

```bash
git add AGENTS.md
git commit -m "docs: require explicit CI evidence in stage reports (CR-3/R2)

- Definition of Done now mandates workflow name, SHA, run link, job results"
```

---

### Task 6: CHANGELOG record of follow-up pass

**Files:**
- Modify: `CHANGELOG.md` (новая запись сверху, БЕЗ bump версии — версия остаётся `0.1.0-mvp`)

- [ ] **Step 1: Add CHANGELOG entry**

Добавить НАД записью `[0.1.0-mvp] · stage 1.1` блок:

```markdown
## [0.1.0-mvp] — stage 1.1 · review follow-ups (CR-1…CR-5) · 2026-09-08

**Версия:** без bump (не этап по ROADMAP — follow-up pass к stage 1.1; версия остаётся `0.1.0-mvp`)

### Статус и следующий шаг

- Сделано: применены замечания ревью Stage 1.1 (veredict APPROVED WITH MINOR FINDINGS): CR-1 (scale ownership синхронизирован в ARCHITECTURE), CR-2 (validate.sh — 4 слоя проверки, включая qmllint), CR-3 (в AGENTS.md §8 — правило явных CI-доказательств), CR-4 (scripts/preview.sh + моки sddm/config), ревью-файл переименован по конвенции `YYYY-MM-DD-stage-X-Y.md` (2026-09-08-stage-1.1.md).
- Следующее: **stage 1.2 — Digital clock + indicator pill** (`0.1.1-mvp`). CR-5 (multi-screen Screen.width/height) — INFO, проверить при stage 3.8 (HiDPI/multi-monitor) или раньше по возможности.
- Блокеры: нет.

### Детали изменений (для агентов)

- `docs/ARCHITECTURE.md:5.1` — scale ownership: scale считает ТОЛЬКО `Main.qml`; `ThemeState` получает `s`; компоненты потребляют через composition/state (CR-1).
- `validate.sh:1-…` — слоёная валидация: required files → metadata.desktop (QtVersion, MainScript/ConfigFile) → theme.conf keys → qmllint (с fallback-флагами) (CR-2).
- `scripts/preview.sh` — превью-раннер Qt6 (qml6 auto-detect), `--mock`, `-W/-H`, `-o` (CR-4).
- `preview/MockSddm.qml`, `preview/MockConfig.qml` — моки для превью (CR-4).
- `AGENTS.md:§8` — требование явных CI-доказательств в отчёте закрытия этапа (CR-3).
- `docs/code-reviews/2026-09-08-stage-1.1.md` — ревью Stage 1.1 (переименовано из `Stage 1.1 Code Review.md`).
- Код темы `theme/onyx/**` НЕ менялся (CR-5 — на будущее, INFO).

### Тех. долг

- Полная инжекция `sddm`/`config` в превью требует C++-хендла или custom qml runner (context-properties). Сейчас `--mock` — best-effort с warning (CR-4, частично).
- CR-5 (multi-screen `Screen.width/height`) — проверить при stage 3.8 / дефицит: один view на экран.

### Принятые решения

- Follow-up pass к Stage 1.1 выполнен сразу после ревью, до старта Stage 1.2 (AGENTS.md §3.3 — follow-up'ы в план следующего этапа).
- Для превью используем `qml6`/`qml`, симметрично проигнорирован `qmlscene` (Qt5.15).
```

- [ ] **Step 2: Sanity**

```bash
grep -c "CR-" CHANGELOG.md | grep -q "4" && echo "CHANGELOG OK"   # или ручная проверка наличия CR-1..CR-4
```

- [ ] **Step 3: Commit**

```bash
git add CHANGELOG.md
git commit -m "docs(changelog): record stage 1.1 review follow-ups CR-1..CR-4"
```

---

### Task 7: Integration — push, PR, merge, cleanup

**Files:** none (git only)

- [ ] **Step 1: Push + create PR**

```bash
git push -u origin stage/1.1-review-followups
gh pr create --base dev --head stage/1.1-review-followups \
  --title "chore: apply stage 1.1 code review follow-ups (CR-1..CR-5)" \
  --body "Stage 1.1 review (APPROVED WITH MINOR FINDINGS) — apply all findings:
- rename review file to 2026-09-08-stage-1.1.md
- CR-1: sync scale ownership in ARCHITECTURE.md (§5.1)
- CR-2: layered validation in validate.sh (files/metadata/config/qmllint)
- CR-3: explicit CI evidence requirement in AGENTS.md §8
- CR-4: preview runner scripts/preview.sh + preview/ mocks
- CHANGELOG record; no version bump (not a roadmap stage) "
```

- [ ] **Step 2: Report to orchestrator**

Return PR URL. Оркестратор приёмку (прогон validate.sh, preview smoke, чтение diff), затем мержит PR в `dev` и закрывает follow-up pass.

---

*Plan · Stage 1.1 review follow-ups · Onyx · 2026-09-08*