# Code Review — stage 0.3

**Repo:** https://github.com/y-tretyakov/onyx-sddm/tree/dev  
**Branch:** `stage/0.3-ci-metadata-themeconf`  
**Reviewer:** orchestrator review (saved on branch for acceptance)  
**Date:** 2026-09-08  
**Verdict:** **Pending orchestrator acceptance** — subs passed verification; final verdict + merge handled by orchestrator.

---

## Summary

| Stage | Goal | Status |
|-------|------|--------|
| 0.3 | CI skeleton + metadata.desktop + theme.conf | Implemented, verified |

Stage 0.3 Phase 0 по ROADMAP: тема стало installable для SDDM (`metadata.desktop` + `theme.conf`),
починены follow-up F1/F2/F3/F6 из code review 0.1/0.2, добавлен CI skeleton, версия → `0.0.3`.

---

## Acceptance criteria check

| Criterion | Result |
|-----------|--------|
| 1. Installable тема: `metadata.desktop` (QtVersion=6) + `theme.conf` skeleton (F3) | ✅ |
| 2. Main.qml: `import QtQuick` (без 2.0), width/height = Screen.width/height, full-screen root, инстанцирует ThemeState, `readonly property real s: Screen.height / 768` передаёт в ThemeState (F1, F6) | ✅ |
| 3. ThemeState.qml: `s` plain property (не через Screen в QtObject), `isPreview` guard, `bgColor` с config/preview-safe fallback (F2) | ✅ |
| 4. validate.sh REQUIRED_FILES включает metadata.desktop + theme.conf (F3) | ✅ |
| 5. CI skeleton `.github/workflows/ci.yml` (validate.sh + qmllint) | ✅ |
| 6. Версия 0.0.3 (README badge + CHANGELOG entry + README status sync, F5) | ✅ |
| 7. Рецензия сохранена в `docs/code-reviews/2026-09-08-stage-0.3.md` | ✅ |

---

## Verification results

- `bash -n validate.sh` → exit 0
- `qmllint theme/onyx/Main.qml` → exit 0
- `qmllint theme/onyx/ThemeState.qml` → exit 0
- `./validate.sh` → `OK: .../theme/onyx`, exit 0
- `./validate.sh /tmp/onyx-03-broken` (без metadata.desktop/theme.conf) → `FAIL` + перечислены оба файла, exit 1
- `python3 -c "yaml.safe_load(open('.github/workflows/ci.yml'))"` → `YAML OK`
- BUMP check (`./validate.sh && grep version-0.0.3 README && grep [0.0.3] CHANGELOG`) → `BUMP OK`

---

## Follow-up closure

- **F1 (HIGH)** — Main.qml full-screen + `import QtQuick` (без 2.0) → **closed** в Task 2.
- **F2 (HIGH)** — ThemeState `s` больше не вычисляется через `Screen` в QtObject; значение передаётся из визуального root Main.qml (Task 2→Task 3). → **closed**.
- **F3 (MEDIUM)** — validate.sh теперь требует `metadata.desktop` + `theme.conf` (Task 4). → **closed**.
- **F6 (LOW)** — Main.qml инстанцирует `ThemeState { id: state }` и красит фон из `state.bgColor` (Task 2). → **closed**.
- **F7 (INFO)** — синоним F3, закрыт вместе с ним.

### Remaining follow-ups (not in scope 0.3)

- **F4 (MEDIUM)** — install.sh не пишет `/etc/sddm.conf.d/` с `Current=onyx`, не проверяет права/зависимости — тех. долг → **RC / stage 4.1** (packaging). Зафиксировано в CHANGELOG 0.3 «Тех. долг».
- CI checksums/`verify-theme.sh` → **stage 4.3** (CI completeness).

---

## Notable decisions (ADR)

- Scale вычисляется в визуальном root (`Main.qml`), где `Screen` гарантирован; ThemeState остаётся единым владельцем значения `s` для всех компонентов (ARCHITECTURE §4.3).
- `bgColor` вынесен в ThemeState (color API) с preview-safe fallback на `#000000` при отсутствии `config`/`sddm`.
- CI skeleton минимальный: validate.sh + qmllint. Checksums/verify — stage 4.3.

---

## Deviations / notes

- Task 7 (push + PR + merge) НЕ выполнялся субагентом — по инструкции orchestrator сам выполняет push/PR/merge после приёмки. Рецензия сохранена.
- `qmllint` локально (Arch) бинарь называется `qmllint`; в CI (`ubuntu-latest`) пакет/бинар — `qmllint6`. Оба корректны для своих окружений (см. ci.yml).

---

*Review stored in docs/code-reviews/ · Onyx · 2026-09-08*
